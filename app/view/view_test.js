'use strict';

describe('calculator.view module', function() {

  beforeEach(module('calculator.view'));

  describe('view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl = $controller('ViewCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});